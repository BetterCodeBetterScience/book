[project]
name = "bettercode-book"
version = "0.1.0"
description = "Text for BetterCodeBetterScience book"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "codespell>=2.4.1",
    "frontend>=0.0.3",
    "jupyter-book>=1.0.2",
    "matplotlib>=3.10.8",
    "mystmd>=1.7.1",
]


[tool.codespell]
# Ref: https://github.com/codespell-project/codespell#using-a-config-file
skip = './book/transcripts,./book/_build/html,.js*,.git*,*.lock,*.bib,.venv*,*.ipynb,*.json'
check-hidden = true
ignore-regex = '\b(BLOSUM|Shapin)\b'
ignore-words = 'project-words.txt'

